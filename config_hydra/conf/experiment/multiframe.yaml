# @package _global_
defaults:
  - override /data/handler: multiframe

data:
  dataset_path: storage/aifs-ea-an-oper-0001-mars-n320-1979-2022-6h-v6.zarr
  graph_provider:
    graph_path: resources/graphs/stage_a/graph.pt
  dataset:
    no_frames: 3
  dataloader:
    num_workers: 0
    persistent_workers: false
    shuffle: false

trainer:
  max_epochs: 1

logger:
  experiment_name: multiframe
