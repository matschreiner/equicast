# @package _global_
data:
  dataset:
    repeat: 1000
  dataloader:
    num_workers: 0
    persistent_workers: false
    shuffle: false

trainer:
  max_epochs: 1000
  callbacks:
    - _target_: equicast.callbacks.step_timer.StepTimer

logger:
  experiment_name: benchmark
