# @package _global_
data:
  dataloader:
    num_workers: 0
    shuffle: false

trainer:
  max_epochs: 1000
  callbacks:
    - _target_: equicast.callbacks.step_timer.StepTimer

logger:
  experiment_name: benchmark
