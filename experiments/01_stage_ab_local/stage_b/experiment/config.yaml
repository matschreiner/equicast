# @package _global_
defaults:
  - override /data: n320

ckpt_path: experiments/01_stage_ab_local/stage_a/checkpoints/initial.ckpt

trainer:
  max_steps: 5000
  callbacks:
    - _target_: equicast.callbacks.checkpoint.TimeDeltaCheckpoint
      dirpath: experiments/01_stage_ab_local/stage_b/checkpoints
    - _target_: equicast.callbacks.step_timer.StepTimer
