# @package _global_
defaults:
  - override /data: n320
  - override /model/backbone: painn_wide

model:
  backbone:
    hidden_dim: 64

logger:
  tracking_uri: experiments/01_stage_ab_local/mlflow
  experiment_name: stage_b

ckpt_path: experiments/01_stage_ab_local/mlflow/989249639477683992/f57859197bae42a69b023636542f42d8/artifacts/checkpoints/latest.ckpt

trainer:
  max_steps: 5000
  callbacks:
    - _target_: equicast.callbacks.checkpoint.TimeDeltaCheckpoint
    - _target_: equicast.callbacks.step_timer.StepTimer
    - _target_: equicast.callbacks.memory_monitor.MemoryMonitor
