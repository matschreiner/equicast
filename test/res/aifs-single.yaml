# Stretched grid graph config intended to be used with a cutout dataset.
# The stretched mesh resolution used here is intended for o96 global resolution with 10km
# limited area resolution.
overwrite: True

attributes:
  edges: &edges_attributes
    edge_dirs:
      _target_: anemoi.graphs.edges.attributes.EdgeDirection
      norm: unit-std
    edge_length:
      _target_: anemoi.graphs.edges.attributes.EdgeLength
      norm: unit-std

nodes:
  data:
    node_builder:
      _target_: anemoi.graphs.nodes.AnemoiDatasetNodes
      dataset: test/res/micro_aifs.zarr

edges:
  - source_name: data
    target_name: data
    edge_builders:
      - _target_: anemoi.graphs.edges.CutOffEdges
        cutoff_distance_km: 170.0
        max_num_neighbours: 64
    attributes: *edges_attributes
