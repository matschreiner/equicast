# @package _global_
defaults:
  - override /model/backbone: painn_minimal
  - override /optimization/scheduler: cosine

data:
  dataloader:
    num_workers: 0
    persistent_workers: false

optimization:
  scheduler:
    warmup_steps: 10
    total_steps: 100

model:
  model:
    compile_backbone: false

logger:
  experiment_name: minimal
