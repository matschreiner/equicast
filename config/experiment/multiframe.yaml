# @package _global_
defaults:
  - override /model/data_handler: multiframe

data:
  dataset_path: storage/aifs-ea-an-oper-0001-mars-n320-1979-2022-6h-v6.zarr
  graph_provider:
    graph_path: resources/graphs/o96/graph.pt
  dataset:
    no_frames: 3
  dataloader:
    num_workers: 0
    persistent_workers: false
    shuffle: false

trainer:
  max_epochs: 1

logger:
  experiment_name: multiframe
